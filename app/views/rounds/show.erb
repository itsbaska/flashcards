<h1> <%= @deck.title %> </h1>

<p> You've completed a round of <%= @deck.title %>. You answered <%= @round.count_first_guess %> / <%= @deck.cards.length %> cards correctly on the first try. You submitted <%= @round.count_total_guesses %> total guesses before each card was answered correctly. </p>
